package TestAnnotations2;

import org.testng.Assert;
import org.testng.annotations.Test;

public class HardAssertions {
	
	@Test
	void test() 
	{
		//Hard Assertions
		
//		Assert.assertEquals("xyz", "xyz1"); //it will all type of values (numbers, strings..)
		
//		Assert.assertEquals(123, 543); //we can compare numbers as well
		
//		Assert.assertEquals("xyz", 123); //We can compare number with string as well
		
//		Assert.assertEquals(123, "123"); //we can compare like this but it will fail
		
//		Assert.assertNotEquals(123, 123); //it will fail because it is same
		
//		Assert.assertNotEquals(123, 689); //this will pass because it's not same
		
//		Assert.assertTrue(true); //It will pass
//		
//		Assert.assertTrue(false); //It will fail
		
//		Assert.assertTrue(1==2);  //It will fail
		
//		Assert.assertTrue(1==1); //it will pass
		
//		Assert.assertFalse(1==2); //It will pass
		
//		Assert.assertFalse(1==1); //It will fail
		
	
//		Assert.fail(); //We can use this to fail the test intentionally
		
		
	}

}
